deployment:
  staging:
    branch: /(feature|fix|chore).*/
    commands:
      - mvn deploy -s config/settings.xml
  production:
    branch: master
    commands:
      - mvn deploy -s config/settings.xml