dependencies:
  post:
    - git config user.name CIRCLE_USERNAME

deployment:
  staging:
    branch: /(feature|fix|chore).*/
    commands:
      - mvn deploy
  production:
    branch: master
    commands:
      - mvn deploy