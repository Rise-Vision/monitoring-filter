dependencies:
  pre:
    - git config --global user.name JENKINS_USERNAME
    - git config --global user.email JENKINS_EMAIL

deployment:
  staging:
    branch: /(feature|fix|chore).*/
    commands:
      - mvn deploy -s config/settings.xml
  production:
    branch: master
    commands:
      - mvn deploy -s config/settings.xml